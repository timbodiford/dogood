{
  "name": "dogood",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ..",
    "postinstall": "npm run build && echo 'Client built!'"
  },
  "engines": {
    "node": "12.x"
  },
  "dependencies": {}
}
